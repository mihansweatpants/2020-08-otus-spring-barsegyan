spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
    schema: shema.sql
    data: test-data.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        format_sql: true

    show-sql: true
    open-in-view: true