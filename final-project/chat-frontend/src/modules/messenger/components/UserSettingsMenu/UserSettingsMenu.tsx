import React, { FC } from 'react';

import { useStyles } from './styles';

interface Props {
}

const UserSettingsMenu: FC<Props> = ({}) => {
  const styles = useStyles();

  return (
    <>UserSettingsMenu</>
  );
};

export default UserSettingsMenu;
